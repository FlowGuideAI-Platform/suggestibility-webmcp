node_modules/
dist/
.wrangler/
package-lock.json

# Sample artifacts and their captured review packages.
#
# These are content, not source. The .md files are the exact bytes submitted
# to the review pipeline, so reflowing them changes the artifact that was
# reviewed and invalidates the word counts the catalogue publishes. The .json
# bundles hold board output that cost real model calls -- reformatting them
# would rewrite a record of what the panel actually returned.
public/samples/
